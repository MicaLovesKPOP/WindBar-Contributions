# Examples

This directory will contain small, maintained reference examples for stable WindBar creator formats.

Planned examples may include:

- Layout Profile
- Visual Theme / Color Scheme
- Start Layout
- Experience Preset
- Plugin/module example once a public plugin SDK exists

No example should be treated as a stable compatibility contract until its associated format is explicitly documented as stable.
